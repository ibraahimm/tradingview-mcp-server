"""Tadawul research feature engine (Polars).

Implements the canonical feature spec (../spec/features.yaml). The golden vectors
(../spec/vectors) are the acceptance gate — see ../spec/conformance/runner_contract.md.
"""
